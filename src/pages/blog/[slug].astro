---
import { getEntryBySlug, getCollection } from 'astro:content';
import "../../styles/global.css";

export async function getStaticPaths() {
  const posts = await getCollection('blog');
  return posts.map(post => ({
    params: { slug: post.slug },
  }));
}

const { slug } = Astro.params;
const post = await getEntryBySlug('blog', slug);
if (!post) throw new Error(`Post not found for slug: ${slug}`);

const { Content } = await post.render();
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{post.data.title}</title>
    <link rel="stylesheet" href="/styles/global.css" />
  </head>

  <body class="bg-white dark:bg-black text-black dark:text-white">

    <!-- Navbar -->
      <nav class="flex justify-between items-center px-6 py-4 shadow-lg sticky top-0 backdrop-blur-md z-50 bg-white/30 dark:bg-black/30">
  <!-- Title -->
  <a href="/" class="text-2xl font-bold tracking-tight">⏱ What's The Time</a>

  <!-- Desktop Nav Buttons -->
  <div class="hidden md:grid md:grid-cols-6 gap-10 items-center px-4 py-2 bg-white/10 backdrop-blur-md border border-white/50 rounded-full">
    <a href="/landing" target="_blank"  class="text-lg font-semibold tracking-tight hover:scale-110">🏠</a>
    <a href="/" target="_blank" class="text-lg font-semibold tracking-tight">⏱</a>
    <a href="/popular" target="_blank" class="text-lg font-semibold tracking-tight">🌍</a>
    <a href="/blog" class="text-lg font-semibold tracking-tight">📚</a>
    <a href="/landing#faq" target="_blank" class="text-lg font-semibold tracking-tight">❓</a>
    <a href="coff.ee/thekusaldarshana" target="_blank" class="text-lg font-semibold tracking-tight">❤️</a>
  </div>

  <div class="flex gap-3">
  <!-- Live Clock -->
  <div class="flex items-center space-x-6">
    <span id="live-time" class="text-2xl font-mono bg-white/10 hover:bg-white/20 p-2 rounded-lg">--:--:--</span>
  </div>

  <button id="theme-toggle" class="p-2 rounded-full border border-gray-300 dark:border-gray-700 bg-white dark:bg-black text-black dark:text-white shadow-md">
          🌓
  </button>
</div>
</nav>

<!-- Mobile Bottom Nav Buttons -->
<div class="fixed bottom-4 left-1/2 transform -translate-x-1/2 z-50 md:hidden px-4 py-2 bg-white/20 backdrop-blur-md border border-white/40 rounded-full flex gap-6 text-lg font-semibold tracking-tight shadow-lg">
  <a href="/landing" target="_blank"  class="text-lg font-semibold tracking-tight hover:scale-110">🏠</a>
    <a href="/" target="_blank" class="text-lg font-semibold tracking-tight">⏱</a>
    <a href="/popular" target="_blank" class="text-lg font-semibold tracking-tight">🌍</a>
    <a href="/blog" class="text-lg font-semibold tracking-tight">📚</a>
    <a href="/landing#faq" target="_blank" class="text-lg font-semibold tracking-tight">❓</a>
    <a href="coff.ee/thekusaldarshana" target="_blank" class="text-lg font-semibold tracking-tight">❤️</a>
</div>

    <main class="prose prose-lg dark:prose-invert max-w-4xl mx-auto px-4 py-10">
      <article>
        <h1>{post.data.title}</h1>
        <p class="text-sm text-gray-500 dark:text-gray-400">
          <em>{post.data.date}</em>
        </p>

        <!-- ✅ This renders the Markdown-formatted content -->
        <Content />
      </article>
    </main>


    <!-- Footer -->
    <footer class="border-t border-gray-200 dark:border-white/10 bg-gray-50 dark:bg-[#111] text-sm py-10 px-4 md:px-12">
      <div class="max-w-7xl mx-auto flex flex-col md:flex-row justify-between gap-6 text-gray-600 dark:text-gray-400">
        <div>
          <p class="font-bold text-gray-800 dark:text-white mb-2">🌐 What's The Time</p>
          <p class="max-w-md">Modern time converter helping global citizens work, meet, and plan across time zones. Made with ❤️ in Sri Lanka.</p>
        </div>
        <div class="flex gap-6 flex-wrap">
          <div>
            <p class="font-semibold text-gray-700 dark:text-white mb-1">Quick Links</p>
            <ul class="space-y-1">
              <li><a href="/" class="hover:underline">Home</a></li>
              <li><a href="/blog" class="hover:underline">Blog</a></li>
              <li><a href="/about" class="hover:underline">About</a></li>
            </ul>
          </div>
          <div>
            <p class="font-semibold text-gray-700 dark:text-white mb-1">Follow</p>
            <ul class="space-y-1">
              <li><a href="https://github.com/kusaldarshana/whatsthetime" target="_blank" class="hover:underline">GitHub</a></li>
              <li><a href="https://twitter.com" class="hover:underline">X (Twitter)</a></li>
              <li><a href="https://buymeacoffee.com/kusal" class="hover:underline">Buy Me a Coffee</a></li>
            </ul>
          </div>
        </div>
      </div>
    </footer>

    <!-- JS -->
    <script>
      // Theme toggle
      const toggle = document.getElementById('theme-toggle');
      toggle?.addEventListener('click', () => {
        const html = document.documentElement;
        const isDark = html.classList.toggle('dark');
        localStorage.setItem('theme', isDark ? 'dark' : 'light');
      });

      // Init theme from localStorage
      const savedTheme = localStorage.getItem('theme');
      if (savedTheme === 'dark') document.documentElement.classList.add('dark');

      // Blog search
      const searchInput = document.getElementById('searchInput');
      const cards = document.querySelectorAll('#postContainer > a');

      searchInput?.addEventListener('input', () => {
        const query = searchInput.value.toLowerCase();
        cards.forEach(card => {
          const text = card.dataset.title;
          card.style.display = text.includes(query) ? 'block' : 'none';
        });
      });
    </script>

    <!-- Live Clock Script -->
    <script is:inline>
      function updateLiveTime() {
        const el = document.getElementById("live-time");
        const now = new Date();
        el.textContent = now.toLocaleTimeString();
      }

      updateLiveTime();
      setInterval(updateLiveTime, 1000);
    </script>
  </body>
  <script defer data-name="BMC-Widget" data-cfasync="false" src="https://cdnjs.buymeacoffee.com/1.0.0/widget.prod.min.js" data-id="thekusaldarshana" data-description="Support me on Buy me a coffee!" data-message="Glad to See you Here! Thank you for Visiting." data-color="#40DCA5" data-position="Right" data-x_margin="18" data-y_margin="18"></script>
</html>
