---
import TimeConverter from "../components/TimeConverter.astro";
import "../styles/global.css";
---

<html lang="en" class="scroll-smooth dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- ✅ Title -->
    <title>What's The Time – Global Time Converter</title>

    <!-- ✅ Description -->
    <meta name="description" content="Easily convert time zones and view current times anywhere in the world with the No #1 Fastest Global Time Converter" />

    <!-- ✅ SEO -->
    <meta name="robots" content="index, follow" />
    <meta name="google-site-verification" content="gKcYaMKLR1jRCwqQfkI-tf-7WGvBxfFvjliDoRtAV8o" />
    <meta name="theme-color" content="#fde047" /> <!-- sunny yellow, matches brand -->

    <!-- ✅ Favicons -->
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <link rel="manifest" href="/manifest.webmanifest" />

    <!-- ✅ Open Graph -->
    <meta property="og:title" content="What's The Time" />
    <meta property="og:description" content="A beautifully crafted global time converter with a modern, sunny UI." />
    <meta property="og:image" content="/og-preview.png" /> <!-- upload this image -->
    <meta property="og:url" content="https://whatsthetime.vercel.app/" />
    <meta property="og:type" content="website" />

    <!-- ✅ Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="What's The Time" />
    <meta name="twitter:description" content="Convert time zones with elegance and accuracy." />
    <meta name="twitter:image" content="/og-preview.png" />

    <script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "WebApplication",
  "name": "What's The Time",
  "url": "https://whatsthetime.vercel.app",
  "description": "Convert time zones globally with a modern, beautiful tool.",
  "applicationCategory": "Utility",
  "operatingSystem": "All",
  "browserRequirements": "Requires JavaScript",
  "inLanguage": "en",
  "author": {
    "@type": "Person",
    "name": "Kusal Darshana"
  }
}
</script>
  </head>


  <body class="bg-[#fbfceb] text-[#1a1a1a] transition-colors duration-300 dark:bg-[#0b0f19] dark:text-white">

    <header class="w-full px-4 py-2 flex items-center justify-between bg-white dark:bg-baseDark shadow-sm">
  <div class="flex items-center gap-2">
    <h1 class="text-2xl font-semibold text-grey-800 dark:text-green-500">⏱ What's The Time</h1>
  </div>

  <!-- 🔘 Theme Toggle -->
    <div class="flex justify-end px-4 py-3">
      <button
        id="theme-toggle"
        class="px-3 py-1 rounded-full backdrop-blur bg-white/10 text-white border border-white/20 shadow-glass hover:scale-105 transition-all duration-300"
      >
        🌓
      </button>
    </div>
</header>


    

    <!-- ⏱ Time Converter -->
    <main class="px-4 animate-fadeIn">
      <TimeConverter />
    </main>

    <!-- 🌙 Dark Mode Toggle Logic -->
    <script>
      const root = document.documentElement;
      const themeToggle = document.getElementById('theme-toggle');
      const userTheme = localStorage.getItem('theme');
      const systemPrefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;

      if (userTheme === 'dark' || (!userTheme && systemPrefersDark)) {
        root.classList.add('dark');
      }

      themeToggle.addEventListener('click', () => {
        root.classList.toggle('dark');
        localStorage.setItem('theme', root.classList.contains('dark') ? 'dark' : 'light');
      });
    </script>

    <style>
      /* Optional green glow effect */
      body::before {
        content: '';
        position: fixed;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 80vw;
        height: 80vw;
        background: radial-gradient(circle, rgba(0, 255, 150, 0.08), transparent 60%);
        z-index: 0;
        pointer-events: none;
      }

      select:focus,
      input:focus,
      button:focus {
        outline: none;
        box-shadow: 0 0 0 2px rgba(34, 197, 94, 0.6); /* Tailwind green-500 */
        border-color: rgba(34, 197, 94, 1);
      }
    </style>

  </body>

  <footer class="mt-10 text-center text-sm text-gray-600 dark:text-gray-400 py-6 border-t border-gray-200 dark:border-white/10">
  <p>&copy; whatsthetime. All rights reserved.</p>
  <div class="flex justify-center gap-4 mt-2">
    <!-- <a href="https://github.com/yourprofile" class="hover:underline" target="_blank">GitHub</a>
    <a href="https://twitter.com/yourprofile" class="hover:underline" target="_blank">Twitter</a> -->
    <!-- <a href="mailto:thekusaldarshana@gmail.com" class="hover:underline">Email</a> -->
  </div>
</footer>

</html>
